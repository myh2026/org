# Issue 文案：实测驱动修复批次 v0.4.17（8 项 checklist）

> 粘贴到 GitHub → New issue（tracking issue）。全部 8 项已随 commit `edbccb1`（分支 `fix/practical-bugs-2026-09-12`）修复并各锁回归用例，合并后可关闭。

---

**标题**：实测驱动修复批次 v0.4.17：expertFixture 分相 / web 只读穿透 / shell 注入面 / stdout 竞态 等 8 项

**标签**：bug / tracking

**正文**：

沙盒全链路实测（bun 工具链装机 · check · demo 全叙事 · TUI 离屏冒烟 · `bun test`）逐项复现后修复，每修一处锁一回归用例。测试 209/209 → 合并用户 feat 分支后 224/224 全绿；回归有效性经 `git stash` 反向验证（旧代码 2 例失败）。

## Checklist

- [x] **expertFixtureOf 无 source 分相**：scripted 直连 minted 专家从必然 `FIXTURE_EXHAUSTED` → 全链路可用；工厂 Register 同步写 direct/handoff 占位轨道。
- [x] **web DELETE/PATCH 穿透 dist/demo 只读约定**：实测可污染入库快照 → 补只读守卫。
- [x] **sh_quote 缺 POSIX 转义**：`commit_registry` 撇号断链 + `dhv_run_gate` 命令注入面 → POSIX 单引号转义。
- [x] **org demo 非默认工作区覆写 dist/demo**：`exportDist` 无守卫 → 仅显式 `--export-dist` 时导出。
- [x] **startRun stdout 捕获竞态**：缺 `await` → 流程结束早于捕获完成。
- [x] **ctx 计量公式双端不一致**：org 与 dhv-ts 各算各的 → 统一口径。
- [x] **cmdReplay 未复用 parseJournalLine**：重放解析与主链路漂移 → 复用同一解析器。
- [x] **测试负例脆弱依赖「环境恰好没装 SDK」**：装了 `z-ai-web-dev-sdk` 的机器上负例假阴 → 新增 `DHV_LLM_DISABLE_SDK=1` 零外联开关（vendored host.ts），负例改机械保证。

## 验证

- `bun cli/org.ts check` 35/35 · `bun test` 224/224 · TUI 离屏冒烟 0 失败 · demo 三连跑全叙事通过。
