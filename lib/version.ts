// ============================================================================
// org/lib/version.ts — 版本单一来源（v0.4.14 漂移治理）
// ----------------------------------------------------------------------------
// 此前 cli/tui/web 三处各自硬编码版本字面量：v0.4.13 批次落地后
// web/entry.ts 仍写着 0.4.12（GUI chrome 徽标落后两个版本）——版本号
// 只允许出现在：本文件（代码唯一来源）· package.json（构建载荷）·
// CHANGELOG/README（叙事文档）。tui/frame.ts 以重导出保持既有
// `import { ORG_VERSION } from "./frame.ts"` 消费面不变。
// ============================================================================

/** CLI banner / web chrome 用（无前缀，拼接处自带格式）。 */
export const ORG_VERSION = "0.5.0";

/** TUI 标题栏用（带 v 前缀）。 */
export const ORG_VERSION_TAG = `v${ORG_VERSION}`;
