# org — 跨平台一致性问题防线
# ----------------------------------------------------------------------------
# 本仓库的补丁锚点（$host.fs.edit 的 old_text 精确唯一匹配）与 fixture 剧本
# （mint_hsl 轨道与 stock 源码逐字一致）都依赖确定的换行符。Windows 检出若
# 转成 CRLF，补丁合入与工厂验收会在 Windows 上静默失败 —— 全部文本资产强制 LF。

* text=auto eol=lf

*.hsl   text eol=lf
*.ts    text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.txt   text eol=lf
*.sh    text eol=lf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# 二进制防误伤
*.png binary
*.ico binary
